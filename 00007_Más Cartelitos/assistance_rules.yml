---
rules:
- !ruby/hash:ActiveSupport::HashWithIndifferentAccess
  when: !ruby/hash:ActiveSupport::HashWithIndifferentAccess
    only_these_expectations_failed:
    - escribirCartelito UsesIf
  then: vas a tener que tomar decisiones, porque dependiendo del cuarto argumento
    tendrás que devolver un cartel corto o un cartel largo; _parece una tarea par
    aun `if` :thinking_face:_
