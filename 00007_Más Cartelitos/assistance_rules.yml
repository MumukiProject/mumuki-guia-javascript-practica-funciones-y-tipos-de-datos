---
rules:
- !ruby/hash:ActiveSupport::HashWithIndifferentAccess
  when: !ruby/hash:ActiveSupport::HashWithIndifferentAccess
    these_expectations_failed:
    - "* Declares:escribirCartelito"
  then: 'en el ejercicio :arrow_backward: anterior definiste `escribirCartelito` y
    en este tenés que modificarlo, así que **no borres su declaración**'
- !ruby/hash:ActiveSupport::HashWithIndifferentAccess
  when: !ruby/hash:ActiveSupport::HashWithIndifferentAccess
    these_expectations_failed:
    - escribirCartelito UsesIf
  then: 'en `escribirCartelito` vas a tener que tomar decisiones, porque dependiendo
    del cuarto argumento tendrás que devolver un cartel corto o un cartel largo; :thinking_face:
    _parece una tarea para un `if`_'
